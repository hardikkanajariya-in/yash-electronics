# Flat node_modules so @astrojs/vercel can copy deps without symlinks (Windows EPERM fix).
# After changing this, run: Remove-Item -Recurse -Force node_modules; pnpm install
node-linker=hoisted
shamefully-hoist=true
package-import-method=copy
